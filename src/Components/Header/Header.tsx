import React from 'react';
import {Navbar} from "../Header/Navbar";

export const Header = ():JSX.Element => {
    return(
        <header className="">
            <Navbar />
        </header>
    );
}
